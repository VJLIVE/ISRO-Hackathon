// export default function MessageBubble({ message, sender = "bot" }) {
//   const isUser = sender === "user"
//   return (
//     <div className={`flex ${isUser ? 'justify-end' : 'justify-start'} mb-2`}>
//       <div className={`max-w-xs p-3 rounded-lg text-sm ${isUser ? 'bg-blue-600 text-white' : 'bg-gray-200 text-gray-900'}`}>
//         {message}
//       </div>
//     </div>
//   )
// }
